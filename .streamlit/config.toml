# [theme] 段已移除 → 完全默认 Streamlit 主题（Boss 2026-05-25：全部还原到最原始）。
# 默认 = 白底 + 默认红强调色 + Source Sans + 16px。

[server]
maxUploadSize = 200
enableCORS = false
enableXsrfProtection = true
enableStaticServing = true

[browser]
gatherUsageStats = false

[client]
showSidebarNavigation = false
